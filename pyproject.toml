[project]
name = "projeto-final"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "andersoncarv", email = "andersoncarv@hotmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.5",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
projeto-final = "projeto_final:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
